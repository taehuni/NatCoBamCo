using UnityEngine;

// [신규 · 최소 스텁] 연구 작업장 건물이 아직 스크립트로 없어서 급한 대로 추가한 파일.
// 연구원 생존자(SurvivorAI.Role.Researcher)가 대기할 위치(Transform)를 제공하는 용도로만 씀.
// 실제 연구 트리/UI 로직은 담당자가 이 스크립트를 채우거나 별도로 만들면 됨. Wall.cs 처럼 빈 스텁으로 시작.
public class ResearchLab : MonoBehaviour
{
}
