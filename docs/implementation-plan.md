# LastShelter 구현 단계

작성일: 2026-09-11

이 문서는 구현 순서와 단계별 완료 기준을 정리한 초안이다. 사용자와 각 단계를 구체화한 뒤 구현한다. 문서 작성 자체는 기능 구현 완료를 뜻하지 않는다.

## 기준과 현재 상태

- 빌드용 씬은 `00_Intro`, `01_Main`, `02_Collection`이다. 이는 사용자 지정 사항이다.
- 현재 작업 폴더의 저장된 파일을 기준으로 작성했다. Unity 메모리의 미저장 상태와 실행 결과는 검증하지 않았다.
- 현재 Main의 목적지는 Collection, Collection의 목적지는 Main이다. 이전 점검에서 발견한 Collection → tie0 연결은 수정되어 있다.
- 빌드 설정에는 Intro가 없고 테스트 씬들이 포함되어 있다. Collection의 현재 GUID와 빌드 설정의 GUID도 다르다.
- 씬과 폰트에 기존 로컬 변경이 있다. 이후 구현 시 보존한다.
- 테스트 씬은 개발 참고용으로 남기고 빌드 대상에서 제외하는 방향을 제안한다.
- 기존 기능을 활용한다. 씬 복제나 전면적인 시스템 교체는 이번 계획의 전제가 아니다.

## 전체 순서

| 단계 | 목표 | 핵심 작업 | 완료 시 플레이 결과 |
|---|---|---|---|
| 1 | 실행과 씬 왕복 | Intro 시작/종료, 빌드 목록, 스폰, 플레이어·카메라·UI 수명, 상호작용 참조 | Intro에서 시작해 Main과 Collection을 왕복할 수 있다 |
| 2 | 탐사와 자원 | 인벤토리 연결, 자원 획득/지출 API, 실제 채집 노드, 수량 표시, 자원 종류 정리 | 채집한 자원이 귀환 후에도 남고 화면에 표시된다 |
| 3 | 건설과 거점 유지 | 건설 카드 연결, 비용 검증/차감, 배치·회전·철거, 미리보기 비전투화, 건물 상태 복원 | 자원을 써서 지은 시설과 체력이 씬 왕복 후 유지된다 |
| 4 | 실제 전투 | 적 프리팹 갱신, 플레이어 피격, 타워·벽·코어 공격, NavMesh/충돌 설정 | 적이 이동·공격하고 플레이어와 시설이 피해를 주고받는다 |
| 5 | 하루 진행과 결과 | 낮/밤 전환, 귀환 규칙, 웨이브 통합 관리·초기화, 패배·다시 시작 | 채집→건설→방어→다음 날을 반복하고 패배 후 새로 시작할 수 있다 |
| 6 | 생존자와 연구 | 구출·배치·씬 왕복, 정비공 수리, 채집가 보너스, 연구 시간·비용·효과 | 생존자를 구하고 연구하면 다음 탐사·방어에 실제 이득이 생긴다 |
| 7 | 플레이 화면과 운영 | HUD 전체 연결, 메뉴 입력/커서 처리, 일시정지·설정·타이틀 복귀, 저장 범위 결정 | 표시와 실제 규칙이 일치하고 플레이를 중단·재개할 수 있다 |
| 8 | 콘텐츠와 빌드 검증 | 필드 배치·난이도·수치, 피격/채집/건설 피드백, 선택한 플랫폼에서 빌드·반복 검증 | 에디터 밖에서도 의도한 게임 흐름이 재현된다 |

5단계 완료를 첫 번째 플레이 가능한 최소 버전의 기준으로 삼는다. 각 단계에 필요한 최소 UI는 해당 단계에서 연결하고, 7단계에서 전체 화면과 메뉴를 정리한다. 파일 저장은 씬 왕복 중 메모리에 상태를 유지하는 것과 별개의 기능이다.

## 1단계 상세: 실행과 씬 왕복

### 목표

`Intro → 새 게임 → Main → 포털 → Collection → 포털 → Main`을 반복해도 플레이어 조작과 상호작용이 유지되도록 한다.

### 화면과 조작 제안

- Intro에는 우선 게임 제목, `새 게임`, `종료`를 둔다.
- 새 게임은 Main으로 진입한다. 로딩 중에는 시작 버튼을 다시 누를 수 없게 한다.
- 종료 버튼은 빌드에서 애플리케이션을 종료한다. 에디터 검증에서는 에디터 자체를 종료하지 않는다.
- 포털은 기존 E키 상호작용을 유지한다.
- 포털 범위 안에서는 목적지 안내를 보여 주고, 벗어나면 해당 안내를 해제한다.
- 씬 이동 중 포털 입력을 중복 처리하지 않는다.
- Intro에서는 커서를 표시하고, 플레이 중에는 기존 카메라 조작에 맞춰 잠근다.
- 이어하기·설정의 완성 화면은 7단계 범위로 둔다.

### 작업 순서

| 순서 | 작업 | 구체적인 변경·확인 대상 |
|---|---|---|
| 1-1 | 씬 등록 정리 | 실제 빌드 목록을 Intro, Main, Collection 순서로 구성. 현재 SceneAsset 기준으로 GUID를 일치시킴. 활성 Build Profile이 별도 씬 목록을 쓰는지도 확인 |
| 1-2 | 씬 이동 경로 정리 | 이미 수정된 Main↔Collection 목적지를 유지. 코드·버튼 이벤트에 남은 테스트 씬 경로 검사. 씬 이름을 여러 곳에서 제각각 관리하지 않도록 정리 |
| 1-3 | 플레이어 수명 확인 | Main의 플레이어·카메라·무기·필수 UI가 함께 유지되는지 확인. Main 재진입 시 새 씬의 중복 플레이어를 제거하고 남아 있는 객체로 참조 연결 |
| 1-4 | 스폰 지점 정리 | Main과 Collection마다 활성 SceneSpawnPoint를 하나씩 유지. 위치·회전·바닥 충돌 확인. 시작 시점과 재진입 시점 모두 의도한 지점에 배치 |
| 1-5 | 상호작용 UI 복구 | Main의 잘못된 PlayerInteractUI 스크립트 참조 복구. 버튼·텍스트·슬라이더 할당. 서로 다른 상호작용 대상이 안내를 덮어쓰거나 지우는 문제 점검 |
| 1-6 | Intro 진입점 구현 | 새 게임 시작과 종료를 연결. 새 세션 초기화의 진입점을 마련하고 중복 실행 방지. 이후 단계의 자원·건설·생존자 상태도 이 진입점에 초기화를 연결 |
| 1-7 | 왕복 검증 | 실제 Intro에서 시작해 Main→Collection→Main을 3회 반복. 카메라·입력·UI·이벤트 중복 및 오류 확인 |

기존 Main의 플레이어·카메라 구조와 PlayerScenePersistence를 우선 활용한다. 세션 시작과 종료를 관리하는 작은 진입점을 두되, 현재 모든 매니저를 하나의 거대한 클래스로 합치지 않는다. 기존 싱글턴들이 같은 오브젝트를 중복 제거 대상으로 처리하지 않도록 각 객체의 수명을 확인한다.

### 주요 대상 파일

- `ProjectSettings/EditorBuildSettings.asset`
- `Assets/00_Scenes/00_Intro.unity`
- `Assets/00_Scenes/01_Main.unity`
- `Assets/00_Scenes/02_Collection.unity`
- `Assets/01_Scripts/ScenePortal.cs`
- `Assets/01_Scripts/SceneSpawnPoint.cs`
- `Assets/01_Scripts/Player/PlayerScenePersistence.cs`
- `Assets/01_Scripts/Player/PlayerInteractUI.cs`
- `Assets/01_Scripts/CameraFollow.cs`
- Intro와 세션 진입을 맡을 신규 스크립트: 이름과 분리는 구현 시 결정

### 완료 기준

- [x] 빌드 목록의 첫 씬이 Intro이며 포함된 게임 씬은 지정된 세 씬이다. (배포 빌드는 아직 실행하지 않음)
- [x] 게임 시작 버튼으로 Main에 한 번만 진입한다. (Unity Play에서 버튼 이벤트 중복 호출 검증)
- [ ] 플레이어가 지정된 스폰 위치에 배치되고 이동·카메라 조작이 된다.
- [ ] 포털 안내와 E키 이동이 양방향으로 작동한다.
- [x] 왕복 3회 후 활성 플레이어·게임 카메라·EventSystem에 중복이 없다. (Unity Play에서 포털 로드 메서드를 호출해 검사)
- [ ] Main 재진입 후에도 나침반·상호작용 UI 등 기존 플레이어를 참조하는 UI가 올바른 대상을 사용한다.
- [ ] Missing Script, 씬 로드 실패, 파괴된 객체 참조 오류가 없다.
- [x] Intro와 플레이 화면에서 커서 상태가 맞고 전환 도중 중복 시작 호출이 발생하지 않는다. (직접 키보드·마우스 조작 검증은 별도)

이번 단계의 통과는 자원 저장·전투·하루 진행의 완료를 뜻하지 않는다. 해당 기능은 아래 단계의 기준으로 별도 검증한다.

## 2~8단계에서 구체화할 내용

### 2. 탐사와 자원

- ResourceInventory를 실제 플레이 흐름에 연결한다.
- 현재 Wood/Metal/Food와 UI에 있는 부품 비용의 불일치를 해소한다. 자원 종류는 구현 전에 결정한다.
- 잔액 확인과 여러 자원의 일괄 차감 API를 만든다. 일부 자원만 차감된 뒤 실패하지 않도록 한다.
- Collection에 선택한 자원 종류를 실제로 배치하고 수량을 표시한다.
- 채집한 노드는 재진입 시 중복 획득되지 않도록 유지한다. 하루마다 재생할지 영구 소진할지는 결정 사항이다.
- 완료 기준: 채집→귀환 후 수량 유지, 부족한 자원 지출 거절, 충분한 자원 지출 시 정확한 차감.

### 3. 건설과 거점 유지

- 실제 구현된 건물부터 건설 카드와 프리팹을 일치시킨다. 설명만 있는 건물은 목록에서 구분하거나 제외한다.
- 미리보기는 공격·수리·피격 대상으로 작동하지 않게 한다.
- 배치 가능 여부, 비용 충족, 생성 성공을 하나의 건설 처리로 연결한다.
- 철거 레이어를 복구하고 철거 환급 정책을 정한다.
- 시설 종류·위치·회전·체력 등 거점 상태를 세션 데이터에 보관한다. 중복 생성 없이 Main 재진입 시 복원한다.
- 완료 기준: 시설을 건설하고 일부 손상시킨 뒤 씬을 왕복해도 같은 상태로 남으며 비용이 한 번만 차감됨.

### 4. 실제 전투

- Main 스포너의 Enemy.prefab을 현재 분리된 적 모듈 구조에 맞춘다. 이동·체력·공격 수치와 행동 컴포넌트를 검증한다.
- 플레이어에게 피해받기·체력 0 상태를 구현하고 적 공격과 연결한다. 결과 화면 전환은 5단계에서 연결한다.
- 일반·원거리·고속·탱크 중 첫 버전에 사용할 적을 정해 각각 역할대로 작동하게 한다.
- 배치한 벽/타워와 적의 NavMesh, 공격 지점, 장애물 판정을 함께 검증한다.
- 완료 기준: 적 이동→시설/플레이어 공격→체력 감소→적/시설 파괴가 실제 플레이에서 성립함.

### 5. 하루 진행과 결과

- GameManager를 낮·밤의 공통 기준으로 삼아 조명과 생존자/HUD 연동 지점을 마련한다.
- 낮의 종료 조건, 필드에 남아 있을 때의 처리, 밤에 포털을 사용할 수 있는지 결정한다.
- 여러 스포너를 하나의 방어 진행으로 집계한다. 모든 생성 완료와 생존 적 수를 기준으로 방어 종료를 판정한다.
- 다음 날 웨이브 상태를 초기화하고 날짜별 난이도 데이터를 적용한다.
- 코어/플레이어 체력 0 시 게임 진행을 종료하고 최소 결과·다시 시작 화면을 연결한다.
- 새 게임에서 날짜·자원·건설·채집 기록 등을 초기화한다. 정상 씬 왕복과 새 게임 초기화를 구분한다.
- 완료 기준: 최소 이틀 연속 진행, 패배 후 재시작, 필드에서 밤 전환 등 경계 상황 통과.

### 6. 생존자와 연구

- Collection에 구출 대상과 필요한 경비 적을 배치하고 SurvivorManager를 연결한다.
- 구출한 생존자의 Main 배치, 이후 씬 왕복, 파괴된 homePoint 재연결을 처리한다.
- 정비공 수리와 채집가 보너스를 연결하고 식량/부상 규칙의 범위를 정한다.
- 연구에 비용·진행 시간·완료 효과·레벨 상한을 연결한다. 연구원 보너스는 실제 연구 시간 계산에 반영한다.
- 완료 기준: 구출 전후의 수리·채집·연구 효과 차이를 확인하고, 재진입해도 구출이나 연구 효과가 중복되지 않음.

### 7. 플레이 화면과 운영

- 날짜·페이즈·남은 시간·웨이브·자원·플레이어/코어 체력을 실제 데이터와 연결한다.
- 탄약·재장전·스태미나는 사용할 규칙을 결정한 뒤 구현하거나 관련 표시를 정리한다.
- 메뉴가 열렸을 때 이동·사격·카메라 입력과 커서 상태를 일관되게 제어한다.
- 일시정지·설정·타이틀 복귀를 연결한다.
- 저장/불러오기가 필요한지, 필요하다면 저장 시점과 복원 대상을 정해 구현한다. 이어하기는 저장 기능 완료 후 노출한다.
- 완료 기준: 화면 수치와 실제 상태 일치, 메뉴 전환 후 입력/시간 정상 복구, 채택한 저장 기능의 재실행 검증.

### 8. 콘텐츠와 빌드 검증

- 정해진 자원·시설·적 구성을 기준으로 배치와 수치를 조정한다. 테스트용 체력·방어 값은 실제 밸런스로 교체한다.
- 채집·건설·발사·피격·파괴에 필요한 최소 시각/소리 피드백을 연결한다.
- 목표 플랫폼을 확인하고 실제 빌드에서 새 게임·왕복·방어·패배·재시작을 검증한다.
- 승리 조건을 채택했다면 그 종료 경로도 확인한다.
- 완료 기준: 새 실행에서 의도한 게임 흐름을 재현하고 치명적인 참조·진행 오류가 없음.

## 결정 사항은 해당 단계에서 좁힌다

현재 확정된 것은 빌드용 세 씬이다. 다음은 구현 순서에 맞춰 구체화할 게임 규칙이며 아직 확정된 기획이 아니다.

| 구체화 시점 | 결정할 내용 |
|---|---|
| 1단계 | Intro 최소 구성과 새 게임 시작 흐름 |
| 2단계 | 자원 종류, 채집 노드 재생 규칙 |
| 3단계 | 첫 건설 목록, 비용, 철거 환급 |
| 4단계 | 사용할 적 종류, 플레이어 피격·사망 규칙 |
| 5단계 | 낮 종료/귀환, 밤 포털, 방어 종료, 최종 승리 유무 |
| 6단계 | 생존자 식량·부상·성장, 연구 목록과 효과 |
| 7단계 | 탄약·스태미나·저장 기능의 채택 범위 |
| 8단계 이전 | 목표 빌드 플랫폼과 배포 범위 |

## 전기·저격 타워 연구 연결 (2026-09-25)

- `feature/Min`에서 기존 ResearchItem, ResearchUI, ResearchCardUI, BuildingSystem과 Main 씬을 수정했다. 새 게임 스크립트는 추가하지 않았다.
- 연구소 E 상호작용과 타워 탭을 사용한다. 전기·저격 연구는 같은 종류의 기존 타워와 이후 건설하는 타워에 적용한다. 미리보기에는 적용하지 않는다.
- 타워의 기존 LevelUp 함수를 사용하며 전기·저격 타워 스크립트와 프리팹 능력치는 변경하지 않았다. 타워 기본 Lv.1에서 연구 3회로 Lv.4에 도달한다.
- 회당 비용은 기존 카드 설정을 사용한다. 전기: 금속 65 + 희귀 금속 35, 저격: 금속 80 + 희귀 금속 40. 비용 상승 규칙과 연구 소요 시간은 추가하지 않았다.
- 재료 부족, 최대 연구 단계, 닫힌 메뉴, 패배·재시작 상태에서는 구매하지 않는다. 같은 프레임 중복 호출과 자원 변경 이벤트의 재진입은 한 번만 처리한다.
- 연구 카드의 비용과 현재·다음 주요 수치를 실제 설정으로 표시한다. 능력치 미리 계산은 비활성 임시 컴포넌트에 동일한 LevelUp을 호출하며 원본 프리팹은 변경하지 않는다.
- 아직 연결하지 않은 벽 연구, 기관총 연구, 대포 연구는 준비 중으로 표시하고 구매를 막는다.
- 기존 플레이어에 연구 상태가 남으므로 정상 씬 이동에서 유지된다. 타워 현재 레벨과 비교해 부족한 단계만 적용하여 중복 보너스를 방지한다. 패배 후 새 게임은 연구 0단계로 초기화한다.

### 이번 검증

Unity Main Play에서 기존 함수와 버튼 이벤트를 호출했다. 실제 키보드·마우스 입력, 배포 빌드, 전투 밸런스 검증은 포함하지 않는다. 재화는 테스트 실행 중에만 지급했고 시작 자원 설정은 변경하지 않았다.

- 재화 0, 메뉴 닫힘, 연결되지 않은 연구, 목록에 없는 연구 항목의 구매 차단.
- 실제 건설 메뉴로 전기·저격 타워를 설치한 뒤 연구 구매. 전기 공격력 2→4 및 감속 30%→35%, 저격 공격력 50→60 및 사거리 20→25 확인.
- 회당 자원 정확한 차감, 같은 프레임 반복 호출 및 자원 변경 이벤트 재진입 시 중복 차감 방지.
- 연구 후 새로 건설한 두 종류 타워에도 동일한 강화 적용. 건설 미리보기 유지.
- Main↔Collection 왕복 2회 후 연구 레벨·타워 능력치·손상된 건물 체력 유지, 중복 효과 없음.
- 연구 3단계·타워 Lv.4 제한, 전기 공격력 8 및 방어 감소 활성화, 저격 공격력 80. 한도 이후 자원 차감 없음.
- 원본 프리팹 기본 레벨·능력치 유지. 연구 메뉴를 연 상태의 패배와 다시 시작 후 이전 타워 제거, 연구 0단계·자원 0 복구.
- 통합 검증 구간 Error/Exception 0. 표시 문구 수정 후 별도 카드 검사에서 24개 텍스트의 넘침 없음, 화면 캡처 확인.
- 테스트 코드는 프로젝트 밖 임시 폴더의 ResearchValidation.cs, ResearchVisualCheck.cs이며 결과는 Research-result.json, Research-visual-result.json에 있다.

### 남은 연구 범위와 보류 사항

- 벽·기관총 연구의 실제 효과와 대포 연구 채택 여부.
- 연구 소요 시간 및 생존자 연구원 보너스 연결.
- 연구와 건설의 자원 비용 밸런스 조정.
- 코어 근처 수리 기능은 사용자 요청으로 보류. 철거 환급도 팀 논의 후 결정한다.

## 생존자 구출과 Main 합류 연결 (2026-09-25)

- 기존 SurvivorRescueEvent, SurvivorManager, GameManager와 Main·Collection 씬을 수정했다. 새 스크립트와 새 생존자 프리팹은 추가하지 않았다.
- Collection에 기존 정비공·채집가·연구원 프리팹을 각각 1명 배치했다. 위치는 정비공 (18, 0.02, -16), 채집가 (-8, 0.02, -11), 연구원 (-60, 0.02, -33)이며 바닥과 주변 충돌체를 확인했다. 원본 프리팹과 기존 채집점은 변경하지 않았다.
- Collection에는 NavMesh가 없으므로 구출 대상의 NavMeshAgent만 씬 인스턴스에서 비활성화했다. 구출 전에는 기존 대기 상태를 사용한다.
- Main과 Collection에 SurvivorManager를 연결했다. 어디에서 Play를 시작하더라도 매니저가 생성되며, 일반 왕복에서는 기존 매니저를 유지하고 새 씬의 중복 매니저를 제거한다. 합류할 씬은 01_Main으로 지정했다.
- E 상호작용은 기존 가까운 대상 선택과 경비 적 레이어 검사를 사용한다. 새 경비 적은 배치하지 않았다. 매니저가 없으면 구출을 처리하지 않으며, 성공한 경우에만 명단과 구출 기록을 남긴다.
- 구출된 생존자는 매니저 아래에서 유지하고 Collection에서는 비활성화한다. Main 진입 시 정비공·채집가는 기존 거주구역, 연구원은 기존 연구실 주변의 유효한 NavMesh 지점에 배치한다.
- Main에서 나갈 때 위치·회전을 보관하고 재진입 때 시설 참조와 NavMesh를 다시 연결한다. 이미 구출한 생존자의 구조 상호작용은 끈다.
- 씬 인스턴스의 고정 구출 ID로 재방문 시 중복 구출을 막는다. 새 Play 및 패배 후 새 게임에서 구출 기록을 비우며, 새 게임은 이전 매니저와 생존자도 제거한다.
- 기존 ResourceNode의 채집가 보너스 연결은 유지했다. 채집가 1명 구출 시 기본 15% 보너스가 즉시 적용되고, 생존자가 Collection에서 비활성화돼 있어도 명단의 보너스는 유효하다.

### 실제 Play 검증

기존 상호작용 함수, 물리 거리 판정, 포털 및 재시작 버튼 이벤트를 사용했다. 물리 E키·마우스 직접 입력 및 배포 빌드 검증은 포함하지 않는다.

- Main 시작 명단 0명, Collection 구출 대상 3명 및 서로 다른 ID 확인.
- 구출 전 목재 기본 수량 40 지급. 임시 적 레이어 콜라이더가 가까이 있으면 구출 차단, 건설 메뉴 중 구출 차단 확인. 실제 적 전투 검증은 이번 경비 조건 검사에 포함하지 않는다.
- 정비공·채집가를 각각 구출하고 반복 호출해도 한 번만 명단에 추가됨. 채집가 구출 후 기본 금속 60이 69로 지급됨.
- 먼저 구출한 2명만 Main 거주구역에 합류하고 NavMeshAgent가 정상 연결됨.
- Collection 재방문 시 구출한 2명은 나타나지 않고 미구출 연구원만 남음. 연구원 구출 후 Main 연구실에 합류.
- 추가 왕복 2회 후 3명 유지, 매니저 중복 없음, 새 Main 시설 참조 정상 연결, Collection에서 생존자 재등장 없음.
- 패배 중 생존자 이동 정지. 다시 시작하면 이전 매니저·생존자 제거, 명단과 채집 보너스 초기화, Collection의 구출 대상 3명 복구. 새 게임에서 채집가를 다시 구출하고 Main 합류 성공.
- 통합 검사 Error/Exception 0. Main에 합류한 생존자의 기존 임시 외형과 지면 배치를 화면 캡처로 확인했다.
- 검사 파일은 프로젝트 밖 임시 폴더의 SurvivorValidation.cs, 결과는 Survivor-result.json에 있다.

정비공의 실제 낮·밤 수리 연결, 연구 시간과 연구원 보너스 적용, 식량·부상 정책과 명단 UI는 이번 범위에 포함하지 않았다.

## 정비공 낮·밤 수리 연결 (2026-09-25)

- 위 생존자 구출 단계에서 남겨 둔 정비공 수리를 연결했다. 승인받은 SurvivorMechanicBehaviour와 SurvivorFreeMove 두 스크립트만 수정했으며 씬·프리팹·건물 체력 코드는 변경하지 않았다.
- GameManager의 NightStart/Defense를 밤으로 사용한다. GameManager가 없는 기존 테스트 씬은 SetNight 방식을 유지한다.
- 기존 설정인 탐색 30m, 수리 거리 2m, 초당 체력 5와 PlayerTower 대상만 유지한다. 손실 체력이 가장 큰 타워를 선택하고 수리 완료·파괴·비활성화 시 대상을 다시 찾는다. 벽·코어 수리는 추가하지 않았다.
- 밤에는 배회 이동을 정리하고 수리를 맡는다. 낮에는 수리 대상을 해제하고 배회를 재개하며, 두 Update 실행 순서에 관계없이 전환한다. 부상·패배·재시작·비활성화 시 수리 이동과 대상을 정리한다.
- Unity Main Play에서 실제 Collection 정비공을 구출한 뒤, 런타임에 생성한 기존 전기 타워 프리팹으로 이동·수리를 확인했다. 약 1.11초에 체력 5.57 회복으로 기존 초당 5와 일치했다.
- 낮 수리 중단/배회 재개, 다음 밤 재개, 최대 체력 제한, 다음 대상 선택, 대상 파괴, 수리 대상 없는 밤, 부상/회복, 일시정지, Main↔Collection 왕복 후 수리, 패배 시 중단을 통과했다. Error/Exception 0, 컴파일 및 두 스크립트 diff --check 통과.
- 검증은 실제 Play의 함수 호출과 NavMesh 이동으로 진행했다. 물리 키 입력·배포 빌드·실제 적과의 교전 밸런스는 포함하지 않는다. 테스트 중에만 EnemyMaker를 비활성화했으며 씬에는 저장하지 않았다.
- 검증 코드와 결과는 프로젝트 밖 임시 폴더의 MechanicValidation.cs / Mechanic-result.json에 보관했다. 연구 시간·연구원 효과, 식량 정책과 명단 UI는 계속 미구현 범위다.

## 연구 시간과 연구원 효과 연결 (2026-09-26)

- 승인한 ResearchItem, ResearchUI, ResearchCardUI 세 스크립트에 연결했다. 새 스크립트·UI 오브젝트 없이 기존 카드를 사용하며, 씬·프리팹·타워 능력치 코드는 이번 작업에서 변경하지 않았다.
- 연구 기본 시간은 회당 30초이고 항목별 researchDuration으로 조정할 수 있다. 동시에 한 항목만 진행하며, 시작 시 비용을 한 번 차감하고 완료할 때 기존 강화 함수를 적용한다. 취소·대기열은 추가하지 않았다.
- 시작 시 기존 SurvivorManager의 연구원 보너스로 소요 시간을 확정한다. 건강한 연구원 1명은 30초를 25.5초로 단축한다. 이후 구출·부상·회복은 진행 중인 연구 시간을 바꾸지 않고 다음 연구에 반영된다.
- ResearchUI의 기존 플레이어 수명 주기를 사용해 메뉴를 닫거나 Collection으로 이동해도 진행한다. 게임 시간 기준이며 Main 연구 메뉴의 기존 일시정지는 유지한다. 카드에는 소요/남은 시간, 메뉴를 닫으면 진행된다는 안내, 연구 중/다른 연구 중 상태를 표시한다.
- 완료 시 기존·보존 중인 타워를 강화하고 이후 건설에도 적용한다. 패배·재시작 중에는 연구를 진행하지 않으며 새 게임의 기존 세션 정리로 진행 중 연구와 완료 레벨을 함께 초기화한다.
- Unity Main Play에서 기존 건설 메뉴로 전기·저격 타워를 설치하고 실제 연구 카드 버튼 이벤트를 사용했다. 기본 연구는 시간 배속 없이 Collection에서 완료했으며 확인 시점의 게임 경과 시간은 약 30.15초였다. 완료 전 능력치 유지와 완료 후 비활성 보존 타워 강화, 귀환 후 신규 건설 적용을 확인했다.
- 연구 진행 중 실제 연구원을 구출해 기존 작업은 30초를 유지하고 다음 작업은 25.5초가 되는 것을 확인했다. 부상 시 다음 작업의 예상 시간만 변경되는 것도 확인했다. 메뉴/전역 일시정지, 왕복 중 진행, 중복 호출·자원 이벤트 재진입, 재료 부족·잘못된 시간·미연결 항목 차단, 최대 단계와 패배·새 게임 초기화를 통과했다.
- 후속 완료·최대 단계 검사는 Play 중에만 시간 배속을 사용했다. 일시정지하지 않는 메뉴 설정도 런타임에만 적용해 열린 카드의 완료 갱신을 검사했다. 물리 키 입력·배포 빌드·밸런스 검증은 포함하지 않는다.
- 컴파일 및 세 스크립트 diff --check 통과, 검증 중 Error/Exception 0. 대기·진행·연구원 적용·최대 단계 카드의 텍스트 넘침 검사를 통과했고 화면 캡처에서 표시를 확인했다.
- 임시 검증 파일은 프로젝트 밖의 ResearchTimerValidation.cs / Research-timer-result.json이다. 벽·기관총 연구 효과, 대포 연구 채택, 식량 정책·명단 UI와 비용·시간 밸런싱은 남아 있다.

## 강화 벽체 연구 연결 (2026-09-26)

- 승인한 범위인 기존 Wall, ResearchItem, ResearchUI, ResearchCardUI와 Main의 강화 벽체 카드 설정을 수정했다. 새 스크립트·프리팹은 추가하지 않았고 BuildingHealth/BuildingDefense의 피해·수리 계산을 재사용한다.
- 강화 벽체는 금속 50·희귀 금속 20, 기본 30초, 연구 1회 제한이다. 기존 연구 시간·연구원 보너스·동시 1개 진행 정책을 공유한다. 수리 기술·충격 완화·자동 보강은 계속 미연결 상태로 둔다.
- 효과는 벽 최대 체력 +30%, 방어력 +5다. 현재 건설 목록의 나무 벽 120/방어 0 → 156/5, 철제 벽 220/5 → 286/10, 강화 벽 360/10 → 468/15다.
- Wall이 적용 여부를 보관해 반복 적용을 막는다. 기존 벽은 잃은 체력을 유지하고 이후 건설한 벽은 강화된 최대 체력으로 생성한다. 건설 미리보기·체력 0인 벽에는 적용하지 않는다.
- Main의 연구 카드에 기존 WoodWall 프리팹을 벽 연구 식별 대상으로 연결하고 비용을 설정했다. 저장 시 이전 단계의 기본 연구 시간 30초가 기존 연구 항목에도 직렬화됐다. 자동 추가된 HUD 폰트/앵커 오버라이드 8개는 작업 전 백업에 없음을 확인하고 Editor API로 제거했다.
- Unity Play에서 기존 건설 메뉴로 벽 3종을 설치하고 각각 체력 60을 손상시켰다. 연구 완료 후 나무 96/156, 철제 226/286, 강화 벽 408/468을 확인했다. 실제 피해 함수로 방어력 증가와 기존 최소 피해 5 적용도 확인했다.
- 연구 시작 시 정확한 비용 차감, 중복 호출·자원 이벤트 재진입 차단, 완료 전 능력치 유지, Collection에서 완료 시 비활성 보존 벽 강화, 새 벽 3종 최대 체력 생성, 반복 왕복 후 손상·능력치 유지, 연구 상한·미연결 카드 차단을 통과했다.
- 코어 체력과 타워가 벽 연구에 영향받지 않음을 확인했다. 이후 전기 타워 연구도 완료되며 벽에 중복 효과가 없었다. 새 게임 후 이전 벽·연구 상태가 제거되고 새 나무 벽이 기본 120/방어 0으로 생성됐다.
- 건설 미리보기 검사에서 기존 코드가 DamageableBuilding을 제거한다는 점을 반영해 임시 테스트를 수정했다. 게임 미리보기 코드는 변경하지 않았다. 최종 통합 검사 Error/Exception 0, 컴파일 및 수정 스크립트 diff --check 통과. 카드의 대기·진행·완료 표시와 텍스트 넘침을 검사하고 화면을 확인했다.
- 검증은 실제 Play에서 버튼 이벤트·건설 및 피해 함수를 사용했고 연구 완료 대기는 런타임 배속을 사용했다. 물리 키 입력·적과의 교전 밸런스·배포 빌드는 이번 범위가 아니다. 임시 검증 파일은 프로젝트 밖 WallResearchValidation.cs / Wall-research-result.json에 있다.

## 기관총 타워 연구 연결 (2026-09-26)

- 승인한 Tower, ResearchItem 두 스크립트와 Main의 기관총 연구 카드 설정을 수정했다. 기존 ResearchUI/ResearchCardUI와 연구 시간·연구원 단축 기능을 재사용하며 새 스크립트·프리팹은 추가하지 않았다.
- 기본 Lv.1에서 연구 3회로 Lv.4에 도달한다. 공격력은 10→15→20→25, 공격 간격은 1→0.9→0.8→0.7초이며 사거리 10은 유지한다. 회당 비용은 기존 카드 기준 금속 70·희귀 금속 35, 기본 소요 시간은 30초다. 수치는 밸런싱 전 임시 설정이다.
- Tower에 LevelUp을 추가하고 기존 UpgradeDamage를 재사용했다. 미사용 UpgradeDelay는 다음 공격 시각만 당기던 처리에서 실제 attackInterval을 줄이는 처리로 수정했다. 타깃 선택과 적 피해 계산은 변경하지 않았다.
- ResearchItem은 기관총의 기본·최대 레벨을 읽고 부족한 단계만 적용한다. 카드의 현재/다음 수치는 비활성 임시 컴포넌트에서 동일한 LevelUp으로 계산하며 원본 Tower.prefab은 변경하지 않는다.
- Unity Main Play에서 실제 건설 메뉴로 기관총을 설치했다. Enemy.prefab의 런타임 복제본을 방어력 0·이동/행동 정지 상태로 놓고 실제 Tower.Update의 연속 공격을 측정했다. 각 단계 4회 공격에서 피해량 10/15/20/25를 확인했다.
- 실측 공격 간격은 Lv.1 약 1.000~1.002초, Lv.2 0.926~0.946초, Lv.3 0.801~0.867초, Lv.4 0.701~0.733초였다. 프레임 단위 실행 오차 범위에서 설정 간격을 따랐으며, 사거리 밖으로 옮긴 적은 추가 피해를 받지 않았다.
- 연구 시작 시 정확한 비용 차감과 중복 호출·자원 이벤트 재진입 차단, 완료 전 능력치 유지, Collection에서 완료 시 보존 타워 강화, 이후 건설 적용, 손상 체력 유지, 반복 왕복 후 중복 효과 없음, 3회 상한 및 카드 표시를 통과했다.
- 기관총 연구가 전기 타워에 적용되지 않음을 확인하고 이후 전기 연구도 정상 완료되는 것을 검사했다. 새 게임에서 이전 타워·연구 상태가 제거되고 새 기관총이 Lv.1/공격력 10/간격 1초로 생성됐다. 미리보기는 기본 능력치와 비활성 공격·충돌 상태를 유지했다.
- 컴파일·수정 스크립트 diff --check 통과, 최종 통합 검증 Error/Exception 0. 대기·진행·최대 단계 카드 텍스트 넘침 검사와 화면 확인을 마쳤다. 연구 완료 대기는 런타임 배속을 사용했지만 공격 간격 측정은 배속 없이 진행했다. 직접 키 입력·이동하는 적과의 전투 밸런스·배포 빌드는 포함하지 않는다.
- 검증 파일은 프로젝트 밖 MachinegunResearchValidation.cs / Machinegun-research-result.json이다. Main 저장 중 새로 생긴 HUD 오버라이드 8개는 백업 대비 확인 후 제거했고 검증용 폰트 캐시와 연결 패키지는 복원했다.

## 낮부터 다음 날·재시작까지 통합 Play 검증 (2026-09-26)

- feature/Min에서 현재 누적 변경을 그대로 검증했다. 이번 단계에서는 게임 스크립트·씬·프리팹을 수정하지 않았다. 검증 코드는 프로젝트 밖 IntegratedLoopValidation.cs, 최종 결과는 IntegratedLoop-result.json에 보관했다.
- Intro의 시작 버튼 이벤트로 Main에 진입해 1일 차·자원 0을 확인했다. Collection에서는 준비 완료 호출이 차단됐으며 정비공·채집가·연구원 3명을 구출하고 기존 자원 4곳을 채집했다.
- 채집가 보너스 포함 실제 획득량은 목재 46·금속 69·식량 12·희귀 금속 40이었다. 추가 지급 없이 기존 건설 메뉴·배치 미리보기·비용 차감으로 나무 벽 1개와 기관총 1개를 건설했다. 이후 잔액은 목재 26·금속 9·식량 12·희귀 금속 5였다.
- 이 잔액으로 기관총 연구는 불가능하며, 시도해도 비용 차감이나 연구 시작이 없었다. 첫날 건설 후 연구까지 가능한 수치인지는 이후 밸런싱에서 결정할 사항이다. 동시 동작 검증을 위해서만 Play 중 금속 61·희귀 금속 30을 추가했다. 영구 시작 자원은 변경하지 않았다.
- 같은 날 Collection 재방문 시 채집 자원과 구출 대상이 재등장하지 않았고 건물·손상된 벽 체력·자원·생존자 명단이 유지됐다. Main으로 돌아온 생존자 3명은 역할별 시설과 NavMesh에 다시 연결됐다.
- 연구원 보너스로 기관총 연구 시간이 25.5초가 되는 것을 확인한 뒤 메뉴를 닫고 준비 완료를 호출했다. 시간 배속 없이 밤 동안 연구가 완료되고 기존 기관총이 Lv.2가 됐다. 밤의 Collection 포털은 차단됐다.
- 실제 Main 스포너 4곳의 첫 적 4마리가 이동하는 것을 확인했다. 움직이는 적 하나를 기관총 근처로 옮긴 후 Tower.Update가 선택한 적에게 피해 10을 주는 것을 관찰했다. 적의 AttackBuilding 함수를 1회 호출해 기관총에 피해 10을 주었고, 구출한 정비공이 밤 동안 수리해 체력 30으로 복구했다. 적이 스스로 타워까지 접근해 공격하는 전체 교전 과정은 이 검사 범위가 아니다.
- 이후 웨이브 진행 확인을 위해 테스트 피해로 적들을 처치했다. 스포너별 5웨이브, 총 100마리 생성 및 처리 후 GameManager가 실제 완료 판정을 통해 2일 차 Gathering으로 전환했다. SkipDefense나 직접 날짜 변경은 사용하지 않았다. 이 결과는 플레이어의 정상 전투로 첫날을 버틸 수 있다는 밸런스 판정이 아니다.
- 2일 차에는 일일 스포너 3곳이 전날과 다른 종류로 활성화됐으며 일반 자원 1곳은 소모 상태를 유지했다. 다시 채집하고 Main에 귀환한 뒤 연구 Lv.1·기관총 Lv.2·타워 체력 30·벽 체력 95·코어 체력 100·생존자 3명 유지와 낮 수리 모드 해제를 확인했다.
- 코어를 파괴해 패배 UI·시간 정지를 확인하고 실제 재시작 버튼 이벤트를 실행했다. 이전 플레이어·인벤토리·건물·연구 UI·생존자 매니저가 제거되고 1일 차·자원 0·연구 0·생존자 0·코어 최대 체력으로 초기화됐다. 주요 매니저/HUD 중복이 없고 입력 사용 상태가 복구됐다. 새 Collection의 자원 4곳·구출 대상 3명·첫날 자원 종류 복구 후 Main 귀환까지 통과했다.
- 첫 검사에서는 특정 적 하나만 추적해 타워가 다른 적을 공격한 경우를 놓쳤다. 게임 코드 대신 검사 코드를 전체 적 체력 관찰로 보완한 뒤 위 전체 흐름을 처음부터 통과했다. 최종 검사 failure 없음, Error/Exception 0이다.
- 물리 키보드·마우스 입력, 일반 플레이의 전투 난이도, 배포 빌드는 미검증이다. 임시 연결 패키지는 검증 전 상태로 복원하며 커밋·푸시는 하지 않았다.

## 첫날 무지원 전투 비교 검증 (2026-09-26)

- feature/Min에서 게임 코드·씬·프리팹·전투 및 자원 수치를 변경하지 않고 Unity Play 2회로 비교했다. 임시 검사 코드는 프로젝트 밖 DayOneCombatValidation.cs이며 결과는 DayOne-passive-result.json / DayOne-assisted-result.json이다. 각 결과 화면도 같은 임시 폴더에 보관했다.
- 두 검사 모두 Intro에서 새로 시작해 Collection의 생존자 3명을 구출하고 자원 4곳을 직접 채집 처리했다. 채집가 보너스로 목재 46·금속 69·식량 12·희귀 금속 40을 얻었다. 추가 자원 지급 없이 기존 건설 메뉴와 비용 차감을 통해 기관총 1개·나무 벽 2개를 설치했으며 잔액은 목재 6·금속 9·식량 12·희귀 금속 5였다.
- 동일 배치는 기관총 (0, 1, 4), 나무 벽 (0, 0.25, 8)과 (0, 0.25, -8)이다. 벽 2개로 코어를 완전히 둘러싸는 구성은 아니며 최적 배치를 증명한 검사는 아니다. 낮의 접근·배치는 테스트 자동화로 처리했지만 밤에는 적을 이동시키거나 직접 피해/처치하지 않았고 시간 배속도 사용하지 않았다.
- Main의 현재 설정은 스포너 4곳, 각 5웨이브×5마리, 생성 간격 3초, 웨이브 간격 2초로 총 100마리다. 기관총은 공격력 10/간격 1초/사거리 10, 플레이어 무기는 공격력 10/간격 0.1초/정밀 사격 퍼짐 0.01/사거리 100을 유지했다.
- **시설만 방어:** 플레이어는 정지·사격하지 않았다. 밤 시작 약 7.67초에 첫 코어 피해, 약 13.78초에 코어 파괴로 패배했다. 1웨이브 중 16마리 생성·2마리 처치·14마리 생존 상태였다. 기관총과 벽 2개는 살아 있었고 관찰 구간 체력도 유지됐다. 해당 배치에서 코어로 접근한 적을 시설 화력만으로 처리하지 못한 결과다.
- **플레이어 자동 조준 사격 포함:** 기존 PlayerShoot.TryFireOneShot을 호출해 실제 발사 간격·탄 퍼짐·Physics.Raycast·피해 처리를 사용했다. 연발 모드로 전환하고 조준 속도는 초당 360도, 목표 변경 후 대기는 0.2초로 두었다. 정밀 사격의 기존 카메라 거리·높이·어깨 오프셋·피치 제한을 사용했으며 CharacterController로 기존 이동 속도에 맞춰 코어 주변을 움직였다. 코어에 가까운 적을 우선 추적하는 자동화이므로 사람의 입력·판단 난이도와 동일하지 않다.
- 사격 포함 조건은 약 101.84초에 5웨이브·100마리를 모두 처치하고 2일 차에 도달했다. 코어 체력 100 유지, 시설 3개 모두 생존, 589발 중 즉시 피해 확인 244회로 기록됐다. 플레이어 피해 2,440은 전체 적 기본 체력 3,000의 약 81%다. 나머지는 타워 피해로 해석되며, 이 배치의 방어는 플레이어 사격 기여가 크다.
- 2일 차에는 금속·식량·희귀 금속이 생성됐다. 다시 채집하고 귀환한 잔액은 목재 6·금속 78·식량 24·희귀 금속 45로 기관총 연구 1회(금속 70·희귀 금속 35)가 가능했다. 연구 비용을 확보했다는 확인이며 이 검사에서 실제 연구를 추가로 시작하지는 않았다.
- DailyResourceSpawner는 각 지점에서 전날 종류만 제외하고 독립적으로 다음 종류를 뽑으므로, 위 2일 차 금속·희귀 금속 공급은 매번 보장되지 않는다. 생존자 3명을 모두 구출하고 특정 배치/자동 조준을 사용한 각 1회 결과이므로 일반 플레이 성공률이나 최종 밸런스 판정으로 확대하지 않는다.
- **후속 조정 후보(미적용):** 먼저 직접 조작으로 첫날 4방향 동시 공격에 대응할 여유가 있는지 확인한다. 어려울 경우 첫날 스폰 타이밍 분산이나 생성 간격 완화를 우선 비교한다. 총 웨이브 수만 줄여서는 약 14초의 초반 패배 압박을 해결하기 어렵다. 건설 비중을 높이려면 첫날 시설 구성 선택 폭과 벽 배치의 실제 경로 지연 효과도 별도로 검토한다. 다음 날 핵심 재료 공급 보장은 팀의 채집/성장 설계 결정 후 조정한다.
- 두 검사 모두 failure 없음, Error/Exception 0. 무지원은 추가 재화·직접 피해·강제 처치·시간 배속이 없다는 뜻이며 물리 입력 플레이나 배포 빌드 검증을 의미하지 않는다. 임시 연결 패키지는 검증 전 상태로 복원하고 커밋·푸시는 하지 않았다.

## 임시 플레이어·쉘터 모델 적용 (2026-09-26)

- feature/Min에서 사용자가 승인한 임시 모델 제작 및 Main 확장을 진행했다. 변경 전 누적 작업을 별도로 백업했으며 커밋·푸시는 하지 않았다.
- Blender로 플레이어·코어·작업장·연구소·거주 시설·외벽·방벽·상자·가로등 9종을 제작했다. FBX, URP 재질, 독립 프리팹, Blender 원본과 제작 도구를 보관했다. 상세 파일과 교체 방법은 temporary-art-guide.md에 기록했다.
- 플레이어에는 16본 리그와 Idle/Walk/Run을 연결했다. 승인받은 PlayerController에 Animator 참조 및 실제 수평 이동 속도 전달 8줄만 추가했다. 새 런타임 스크립트는 없으며 기존 이동·회전·사격 계산은 유지했다.
- Main 내부를 48×48m, 사방 출입구를 폭 8m로 배치하고 시설·수집 단말·스폰 위치를 조정했다. 기존 시설 기능 루트와 비활성 이전 외벽을 유지했다. 모델에 맞춰 충돌 범위를 설정하고 별도 NavMesh를 구웠다. Collection은 작업 시작 시 파일과 동일하다.
- Unity Play 자동화에서 애니메이션 3종 선택/관절 변화, 네 출입구 실제 이동, 씬 왕복 시 플레이어/Animator 유지, 구출 생존자 3명 시설 배치, 새 연구소 상호작용, 실제 자원 차감 건설, 네 방향 실제 적의 입구 통과를 확인했다. Error/Exception 0, 최종 컴파일 오류 없음이다.
- 플레이어 스튜디오 렌더와 Unity 전체 배치·Play 화면을 ArtSource/PrototypeShelterV1/Previews에 저장했다. 임시 검증 폰트 캐시와 연결 패키지는 작업 전 상태로 복원했다.
- 직접 키보드/마우스 조작 및 배포 빌드는 미검증이다. 옆/뒤 이동·점프 전용 애니메이션, 조준 IK, 사격/재장전 동작과 최종 텍스처는 후속 작업이다. 작업장은 현재 시각적 배경이다.
- 맵 크기와 적 출발 위치 변경으로 기존 첫날 전투 측정은 다시 필요하다. 적 수·웨이브·공격력·자원 비용은 변경하지 않았다.

## master 변경 통합 검토 (2026-09-26)

- 현재 누적 연구·생존자·임시 모델 작업을 feature/Min의 5c8717f2로 먼저 커밋했다. 원격 master 2dfda2d1을 같은 브랜치에 병합했다. 원격 푸시는 하지 않는다.
- 들어온 변경은 tie0/tie1 작업 씬, 자원 지역 맵·프리팹·재질, 적 Link 낙하/도약과 순찰·추격 개선, ProBuilder/ProGrids 및 NavMesh 설정이다. Main/Collection 지형이나 MainEnemy 프리팹은 원격 변경에 포함되지 않는다.
- 유일한 텍스트 충돌은 NotoSansKR-Bold SDF의 자동 생성 글리프/아틀라스 캐시였다. 원본 폰트 참조와 동적 생성 설정이 있는 우리 쪽 파일을 유지했다. 게임 코드 충돌은 없다.
- master가 추가한 tie0/tie1 빌드 항목은 기존 합의에 따라 제외하고 Intro/Main/Collection만 유지한다. 작업 씬 파일 자체는 보존한다.
- 적은 최초 감지에 시야를 사용하지만 추격 시작 후에는 벽 뒤에서도 추적 해제 거리 안의 플레이어를 계속 추적하도록 바뀌었다. 이를 임의로 되돌리지 않았다. Main은 별도 MainEnemy(기존 Humanoid)와 자체 NavMesh를 사용해 master의 공용 Enemy 프리팹 Agent 유형 변경과 분리된다.
- 병합 후 Unity 컴파일 통과. Main Play 자동화에서 애니메이션 3종, 사방 출입구 이동, Main↔Collection 플레이어 유지, 구출 생존자 3명 복귀, 연구소 상호작용, 목재 46→26의 나무 벽 건설, 네 방향 실제 적 이동을 통과했다. 검사 중 Error/Exception 0.
- Intro/Main/Collection/tie1 씬에서 누락 스크립트 0, 활성 렌더러의 누락·오류 재질 0을 확인했다. tie1의 적은 HP 30/이동 속도 3.5, 순찰 및 Link 컴포넌트와 NavMesh가 연결되어 있다. 새 맵은 아직 tie1에 있고 포털도 tie0으로 향한다. 우리 Collection에 지형을 옮기는 작업은 별도 단계다.

- tie1 실제 Play에서 15초 동안 적의 순찰 이동과 NavMesh 유지, Error/Exception 0을 확인했다. 관찰 구간에 Link 통과는 없었으므로 낙하·도약 전체 경로 검증으로 확대하지 않는다. 직접 키보드/마우스 및 배포 빌드는 미검증이다.
